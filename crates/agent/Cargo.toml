[package]
name = "Minerva-Agent"
version = "0.1.0"
edition = "2024"

[dependencies]
rig = { version = "0.39.0", features = ["lancedb"] }
tokio.workspace = true
protocol = { path = "../protocol" }
futures.workspace = true
async-stream = "0.3.6"
